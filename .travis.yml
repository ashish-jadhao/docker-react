sudo: required
language: generic
services:
  - docker

before_install:
  - docker build -t ashishjadhao/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true ashishjadhao/docker-react npm run test -- --coverage